import React from 'react';

const SearchDeatils = (props) => {
    
   
    return (
        <div style={{marginTop:'10px'}}>
            <img src={props.name} alt="search" />
        </div>
    );
};

export default SearchDeatils;